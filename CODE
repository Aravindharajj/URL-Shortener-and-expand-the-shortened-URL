import pyshorteners
long_url=input("Enter the URL : ")
tiny=pyshorteners.Shortener()
short_url=tiny.tinyurl.short(long_url)
print("The Shortened URL is: "+ short_url)
long=tiny.tinyurl.expand(short_url)

print("Long URL is :" + long)
